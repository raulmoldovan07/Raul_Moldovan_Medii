@page
@model Raul_Moldovan_Medii.Pages.AppointmentServices.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentService[0].Appointment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentService[0].ServiceType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentService[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentService[0].PriceAtBooking)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AppointmentService) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Appointment.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceType.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceAtBooking)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
